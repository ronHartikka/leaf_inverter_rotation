// PLACEHOLDER for characterize_load.ino
// Not attached in the chat that scaffolded this repo. Drop your real sketch here.
// When you do: replace the inline pin map / safe-startup / auto-zero with:
//   #include "../shared/pins.h"
//   #include "../shared/safe_startup.h"    // call relaysSafeStartup() first in setup()
//   #include "../shared/current_sense.h"
// so the boilerplate lives in ONE place and can't fork/drift.
void setup() {}
void loop() {}
