# Raw captures are large and local (data/ has its own too, belt + suspenders)
*.csv
*.raw
# Arduino build artifacts
build/
*.hex
*.elf
# Python
__pycache__/
*.pyc
.venv/
.DS_Store
# Real WiFi credentials -- never commit (template: wifi_secrets_example.h)
firmware/shared/wifi_secrets.h
